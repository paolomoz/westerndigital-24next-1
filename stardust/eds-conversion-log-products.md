# EDS conversion log — products worker rows (agent id `products`)

Merged into `stardust/eds-conversion-log.md` by the coordinator when the wave closes. Brief: `stardust/.work/deploy/briefs/products.md`. Progress: `stardust/.work/deploy/progress/products.log`. Inventory claim appended to the shared log § Block inventory (2026-09-21T11:44Z).

## Blocks written (Step 8)
| block | tier | variants | pages | files | notes |
|---|---|---|---|---|---|
| `hero` | template-slotted | `banner` (products), `photo` (solutions hero), `split` (company hero), `band` (solutions innovation) | products, solutions, company | `blocks/hero/hero.{js,css}` | ONE shared core; query-based decode (#42): picture 1 = bg layer (eager LCP, `fetchpriority=high`, min-heights reserved), picture 2 (split) = media column, h1 → `.headline`, band title = first link-free `<p>` (bold paragraph on the source), link `<p>` → `.actions` (banner: plain text link; band: `<em><a>` secondary). CSS lifted verbatim from products.css `.plp-hero*`, solutions.css `.sol-hero*` / `.sol-section` + `.sol-innov*`, company.css `.co-hero*` / `.co-h1` / `.co-h5`. Authoring contract for the solutions/company workers is in the JSDoc; proven against their served prototypes (see Gates). |
| `breadcrumbs` | template-slotted | — | products, solutions | `blocks/breadcrumbs/breadcrumbs.{js,css}` | ONE row, ONE `<ul>` (last `<li>` plain text). Chevron = CSS `::after` data-URI of the source svg. D1 BREADCRUMB 🟡 accepted (conversion log § Step 2). |
| `filters` | reconstructive | — | products | `blocks/filters/filters.{js,css}` | one row per group `[title p \| ul]`; FACET when option labels end with a count `(39)`, else shop-by NAV group (first one open at rest, lifted). EW7: facet title in a sibling div, chevron-only `<button aria-expanded>`; shop-by toggle `a.accordion` wraps the moved `<p>` (proto parity: the title is a link there). Drawer states via body classes `mob-shop-by-open` / `mob-filters-open`. In-code UI constants (D14): "Shop", "Filter", "Filter by", "Clear All", "+ More", "- Less". |
| `product-listing` | reconstructive | — | products | `blocks/product-listing/product-listing.{js,css}` | rows classified by content: count `<p>`; product `[picture \| h3(a) + p capacity + p price (+ p promo w/ link)]` (card-as-link, EW6 unwrap); warranty modal row `[picture \| 4 p]` (picture + paragraphs, no heading); pagination `<ul>` (active page + disabled PREV derived from `?page`, default 1). Sort `<select>` (6 in-code options), mobile Shop/Filter buttons, ✕ close — D14 constants. Promo "Learn More" opens the modal (block-owned; href falls back to the warranty page). Compare control not rendered (dyn #28 drop). |

## Content page (Step 9)
- `content/products.html` — 4 sections: [metadata + hero.banner] · [breadcrumbs] · [filters + product-listing + section-metadata `split-aside`] · [h2 default content + tiles.buy + foot p default content]. Title / Description = the CAPTURED `<title>` / meta description (`stardust/current/pages/products.json`). 21 authored `<img>` (hero bg 3000 rendition, 15 product thumbs 319, warranty modal 1280, 4 tile svgs) — all CAPTURED absolute source URLs (`_media-manifest.json`). Links: `/` (breadcrumb Home), everything else absolute on `https://www.westerndigital.com` (facets, pagination, product pages — links decision row; `/products?query` is NOT the migrated page).
- **Generator recipe (reproducible):** `node stardust/.work/deploy/_products-view.mjs` → `stardust/.work/deploy/_products-view.html` (generator INPUT + round-trip BASELINE) and `_products-patches/products.json`; then `prototype-to-content.mjs stardust/.work/deploy/_products-view.html --schema stardust/eds-schema/products.json --out content/products.html --slug products --patches stardust/.work/deploy/_products-patches --map hero=block:hero --map breadcrumbs=block:breadcrumbs --map listing=block:product-listing --map buy-direct=block:tiles [--force]`.
  - Why a derived view: the served prototype carries no `data-section` (the schema names come from the coordinator's schema-view copy — `stardust/.work/deploy/schema-view.mjs`); the view is that copy with the RECORDED drops applied (5 hidden hero variants, 15 compare buttons + tray, 4 duplicate warranty modals, hidden shells, empty `/products/clearance` li, hidden "(0)"), captured image URLs, `btn-link` → `text-link` (text links, not buttons), trailing `<br>` artefacts removed, the hero background materialised as an editorial `<picture>`, the trail as one `<ul>`, links absolutised.
  - Why patches: the generator emits ONE block per section; the locked `listing` shape is TWO blocks (`filters` + `product-listing`) in one `split-aside` section → transcribed from the same DOM into the generator's patch file (`replace` on `main > div:has(div.product-listing)`); Title/Description replaced with the captured values; `tiles` gets its `buy` variant by an `attr` patch (`--map` refuses a variant token). **Coordinator note:** three generator gaps worth a feature — multi-block sections, `--map name=block:<name> <variant>`, captured-title metadata; and `products.json § hero.repeats` (6 = the 5 hidden variants) should be pruned to 1 (qa-gate `units` reads it).

## Gates
| gate | result |
|---|---|
| `block-lint.mjs blocks/ --styles styles/styles.css` | exit 0 — 0 🔴 on the tree; owned blocks 0 🟡 (the 8 🟡 are the chrome blocks, recorded by the foundation worker) |
| `block-roundtrip.mjs <view> content/products.html --blocks hero,breadcrumbs,filters,product-listing --map … --strict` | exit 0 — 0 structural 🔴; EW editable 80/80, dead 0, duplicated 0, exempt 0; product-listing 4 🟡 (count "89"/"Items" spans authored as one `<p>89 Items</p>`) |
| whole-page `block-roundtrip` (`--strict`, `--ew`, all blocks incl. tiles rows) | exit 0 — 0 🔴, EW 93/93 |
| hero variants vs the SERVED sibling prototypes: `_products-hero-solutions.html` (photo + band, `--map "hero=.sol-hero, .sol-innov"`), `_products-hero-company.html` (split, `--map hero=.co-hero`) | exit 0 / exit 0 — 0 🔴, EW 5/5 + 2/2; 🟡 IMG COUNT only (the prototypes paint the photo as a CSS background, the block authors it as an editorial `<picture>`) |
| `davids-model-lint.mjs content/products.html --icons-dir icons --styles styles/styles.css` | exit 0 — PASS 0 🔴, 3 🟡: D1 BREADCRUMB (accepted), D3 product-listing rows with 1 vs 2 cells (locked shape: count / pagination rows are single-cell), D4 4 authored SVG tile icons (pure-vector 2.7 KB, for media-reconcile) |
| `ew-editability-probe.mjs --content content/products.html --simulate-editor --verbose` | exit 0 — 93/93 editable, 0 dead, 0 dup; owned blocks: drift 0, editDrift 0, blockΔh 0. (`tiles` shows CTA drift 42→24 on its UNDECORATED rows — the block is the solutions worker's, not yet written.) |
| `build-harness.mjs` + `aem up` (port 3114 — the sibling's harness, reused; not started or stopped by this worker) + `qa-gate.mjs … --schema stardust/eds-schema/products.json` | exit 1 — 38 ok, 6 warn, **1 fail**: `units: "hero" → block hero renders ≥6 — rendered 1` = the schema's `repeats` count includes the 5 hidden use-case variants that are RECORDED drops (conversion log § Step 2; the view/page author only the default variant). Everything else green: 1 h1, 0 pageerrors, 0 broken images, every block loaded + non-empty, 16 controls driven (all 10 facet/shop-by toggles change `aria-expanded`), units breadcrumbs 2 / listing 52 / buy-direct 15, h1Section ok. Warns: 4 dead header controls (foundation), wide-1600 hero/tiles full-bleed (correct: bg full-bleed, content re-contained). Re-run on the final code (own `aem up` on the port.mjs slot 3114 after the sibling's server had stopped; stopped again with `port.mjs stop harness`): identical — 38 ok / 6 warn / 1 fail. |
| `render-harness.mjs content/products.html … --fragments content/` | exit 0 — block errors [] → `stardust/.work/deploy/harness/products.png` |
| geometry vs the REAL served prototype (`stardust/.work/deploy/probes/_products-heights2.mjs`, 1440 + 360, fonts settled) | hero 260/223 = proto; breadcrumbs 24 at the same y; split-aside starts y429/386 = proto `.plp-app`; toolbar 39/109, promo 192, pagination 48/52 = proto. Residuals: product card −24/−25 px per card (dropped compare control, recorded), grid −96 px at 1440 (4 rows × 24), sidebar −16 px (empty clearance li dropped). |
| `code-sync-verify.mjs --lint --files <8 files>` | exit 0 — 8 files clean (eslint 0 / stylelint 0) |

## Images needing rehost (401/403)
- none found: the manifest recorded 200 (in-page) for every authored URL; a browser-UA `curl` sample (hero 3000, a product 319 thumb, the warranty 1280, a tile svg) returned 200 image/* with byte sizes matching the captured copies. Capture technique was `headless`; the definitive pre-flight is the coordinator's `media-reconcile.mjs` / `rehost-media.mjs` run. Advisory: 4 authored `.svg` (tile icons, pure-vector, 2.7 KB each).
- Mobile renditions (`all-products-mobile 840`) are not in the manifest → the 3000 rendition is the single authored source; the pipeline generates responsive variants.

## Decisions / residuals (D8 ledger)
- Compare control (24 px per card) and compare tray: not authored (conversion log § Step 2, dyn #28) — the only main-column geometry delta vs the prototype.
- Empty `/products/clearance` link in "Shop by Category": dropped (16 px sidebar residual).
- 4 duplicate `#warrantyCtaModal` copies: one modal authored (row in product-listing); promo "Learn More" targets `/company/programs/extended-warranty` (absolute) as the no-JS fallback.
- "89 Items" authored as one paragraph (the source splits "89" and "Items" into spans).
- The five hidden hero variants (PLP filter states): not authored — also the cause of the one qa-gate fail (schema `repeats`).
- `text-link` rename in the view: the source `.btn-link` / `.btn-link--medium` are underlined text links; the generator would otherwise emit `<em><a>` (buttons).
- ew-editability-probe's runtime mimic buttonises ANY sole link (`a.className = 'button'`), unlike this project's formatted-only `decorateButtons`; the banner CTA rule resets padding/border/background so the text link renders identically either way (drift 0).
- Not touched: `blocks/tiles` (solutions worker) — the page authors `tiles buy` rows to the inventory shape `[picture | h3 + p + link]`.
