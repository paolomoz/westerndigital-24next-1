# EDS conversion log — index worker (agent id `index`)

Fragment merged by the coordinator into `stardust/eds-conversion-log.md` when the wave closes. Owns: `carousel`, `tabs`, `cards` (content/promo/resource/about), `columns` (feature/split/cta — shared block, all three variants), `content/index.html`. Probes/helpers: `stardust/.work/deploy/_index-*` · progress: `stardust/.work/deploy/progress/index.log`.

## Inventory rows owned (claimed in the shared log § Block inventory)
| block | variants | tier | authored shape | status |
|---|---|---|---|---|
| `carousel` | `hero` | reconstructive | one row per slide: [picture (optional; none → black ground, no scrim) \| `<p>` title + `<p>` lede + `<p><em><strong><a>` CTA (accent = source white button) \| `<p><strong>` progress title + `<p>` desc] — 4 rows | done |
| `tabs` | `category` | reconstructive | h1 default content before the block (styled in place, no reabsorb); one row per tab: [`<p>` label \| `<ul>` of `<li><a><img>label</a>` tiles + `<p><em><a>` See-All CTA] — 3 rows (5/6/4 tiles) | done |
| `cards` | `content`, `promo`, `resource`, `about` | reconstructive | h2 default content before the block; one row per card [picture (optional) \| text]: content `p eyebrow + h3 + <em><a>`; promo editorial `<p><strong>title</strong></p> + p + <p><a>` text link, promo product `<p><a>name</a></p> + Capacity/Starting-at p's with <strong> values` (whole-card link, EW6); resource `p eyebrow + h3 + <p><a>` text link; about `<p><a>label</a></p>` (whole-tile link, EW6) — 7/5/6/5 rows | done |
| `columns` | `feature`, `split`, `cta` | reconstructive | feature (index): h2 + lede default content, one row [picture + h3 + p \| picture + h3 + p], `<p><strong><a>` CTA default content after; split (company): one row [h2 + p + CTA \| picture] or [picture \| h2 + p + CTA] (media-first row tagged `.media-first`); cta (solutions): one row [`<p>` title \| `<p><strong><a>` + `<p><em><a>`] | done (feature proven on index; split/cta CSS lifted from company.css / solutions.css, proven by the owning workers' pages) |

## Block notes (decode, EW, deviations recorded)
- **carousel.hero** — clones: 2 each side as the source Splide loop (rest-state list `translateX(-200%)` = lift -2880 @1440); clones are IMAGE-ONLY (no text/alt/href, `stripInstrumentation`, `aria-hidden`) per #100/EW4 — the forward wrap slides into the image-only clone then jumps to slide 1 (0.4 s, the only motion difference vs the source). Autoplay 6000 ms / 50 ms tick, fill 0→100 %, `is-moving` 0.4 s ease; off under `prefers-reduced-motion`, paused while `document.hidden`, interval self-clears when the block is detached (EW9). Progress items are `div[role=button][tabindex=0]` (source shape; EW7 — never `<button>`); labels move as their `<p>`s. Slide titles authored as `<p>` (source `p.h1-hero`, no heading semantics; the page `<h1>` is in the tabs section per schema `hasH1`). Hero bg alt="" (background behind text — decorative; lint TEXT 🟡 accepted). Bug found by the round-trip and fixed: an EMPTY picture cell (slides 3–4) shifted the copy/label cells → `textCells` now ignores empty cells.
- **tabs.category** — `div[role=tab]` controls with the label `<p>` moved inside (EW7), arrow-key + Enter/Space, `hidden` on inactive panels, first tab active (observed); the authored `<ul>` is the grid (one editable unit — the fourth EW5 shape avoided) styled by element (`.tabs .grid ul/li/a/picture/img`), tile pictures `display:inline; vertical-align:middle` inside a block `<picture>` (mirrors the source's inline `img` descender). Band padding/centering ride `main .section.tabs-container` (.section specificity, block CSS). Tile alt="" as on the source (label sits next to the image) — lint TEXT 🟡 accepted. `pointer-events:none` on the active tab is lifted, so qa-gate's "tab-1 no observable change" is expected.
- **cards** — one decode: heading → `.title`, pre-heading p → `.eyebrow`, `<strong>`-led first p → `.title` (+ `.editorial`), FIRST link-only p with no heading → whole-card link (`<a class="card linked">`, inner anchor unwrapped, EW6), later link-only p → `.actions` (moved `<p>`, EW3), remaining p → `.body` / `.meta` (product capacity + price; muted label, bold value). **Navy**: a content card WITHOUT a picture takes `--c-navy` (brief). NOTE the prototype's two `ccard--navy` cards (1–2) also carry opaque images (alpha probe `_index-alpha.mjs`: 0 % transparent px), so navy is invisible there, and the two image-less cards (6–7) are transparent on the prototype but off-screen at 1440 and 360 (rail 150 + 416 × 5 > viewport) — the brief's rule is applied, un-gated. **promo spacer**: the source rail ends with an empty 64 px `pcard--empty` (Splide lastSlide) → presentational `li.spacer` (no words) so the schema count (6) and the flex-children count agree; 500 px min-height at ≤767 (lifted card height). Rails: `overflow: hidden auto`, `margin-left 150 / max-width calc(100% − 150px)` (16 at ≤767), arrows not rendered (dead on live). Band padding + grey grounds ride `main .section.cards-container(:has(.cards.content|.resource))`; the about head is the narrow (992) head. Mobile button padding 8 16 (index.css 360 ramp) written per block since the foundation only sets 14/14 — **foundation request**: `a.button` ≤767 `padding: 8px 16px` would let the three per-block overrides go.
- **columns** — feature/split/cta share one decode (media-only cell → `.media-col`, else `.text-col` with `.title` / `.body` / `.actions`); split's two source layouts (leadership text|media vs responsibility media|text) are distinguished by the authored order (`.row.media-first` = co-cr rules: col padding 0 16, centred; else co-lead: copy padding-right 32/64, media-first stacking below 768). `.co-btn` paint (outline black, 8 16 → 12 40, mt 16 → 24, mr 24 ≥768) is written on `.columns.split .actions a:any-link` regardless of `<strong>`/`<em>` (the source button is the outline style — variant-owned paint, #25). cta: `.sol-cta__row` border-top `rgb(225 225 225)` written as the literal (solutions-page token `--sol-border`, not a global token — no request needed), buttons 8 24 → 12 48, gap 20 (source margin-right 20 on the first). Section-level company/solutions band padding (`.co-lead` 48/64 + mb 32, `.sol-cta` none) is NOT in this block — the owning workers' pages decide it; a `main .section.columns-container:has(.columns.split)` rule is an additive request to me if needed. `.columns.split .body > * + *` carries the 20 px paragraph rhythm at wrapper level (block-lint EW-RHYTHM resolved).
- Shared: `stripInstrumentation`/`wrapNode`/`cellNodes` copied per block (no shared import); nothing measured in `decorate()`; no words added by JS (aria-labels only: "n of N" on slides/units, roledescription).
- block-lint 🟡 accepted: none of mine remain (the 8 tree 🟡 are header/footer EW-RHYTHM/EW-CLASS, foundation-recorded).

## Content page `content/index.html`
- Generated by `prototype-to-content.mjs` from `stardust/.work/deploy/schema-view/index.html` (the only document carrying `<main>` + `data-section` — the served prototype has neither, so the generator finds one `section.mainContainWrap`; the schema's own `source` is this file, `<base href>` to the served origin) with `--map` per section to `block:carousel|tabs|cards|columns`, `--drop .splide__slide--clone --drop .quickView-modal --drop .hero__progress` (hidden-DOM parity clones / hidden quick-view modals / the progress bar re-authored into the carousel rows), `--slug index`, ledger `stardust/.work/deploy/transcribe.json`.
- **Patches** `stardust/patches/index.json` (7 `replace` ops, one per section, written by `stardust/.work/deploy/_index-patches.mjs` from the served prototype DOM + `_media-manifest.json` + captured `<title>`/meta description; applied LAST by the generator per its idempotent-writer contract — never a hand edit of the output). Why: the generator's repeat-unit emitter cannot express the locked shapes (CSS `background-image` slides/cards → picture cells, `<button>` tab labels → tab rows, whole-card links, progress labels, nested `<p><p>` CTA shape, variant classes, relative `assets/media` srcs, Title from the h1 instead of the captured `<title>`). Re-run: `node stardust/.work/deploy/_index-patches.mjs && node …/prototype-to-content.mjs <same args>` (see `progress/index.log` step9 line for the exact command).
- Metadata: Title = captured `<title>` "High-Capacity HDDs for PCs, NAS, Gaming, Data Centers, and AI Data Cycles | WD", Description = captured meta description. One `<h1>` (tabs section, default content). Links: `/`, `/products`, `/solutions`, `/company` root-relative; every other href absolute to https://www.westerndigital.com (or its blog/investor host as captured). Images: 40 authored `<img>` (36 unique), every src = the CAPTURED absolute URL; all 200 in the manifest (10 tab-panel PNGs were coordinator re-harvested with a browser UA, status field unset, local copies present); 6 SVG icon references are pure-vector (768 / 952 bytes, no `<image`/`data:`).
- Not authored (decisions): hero Splide clones (presentational), 4 hidden quick-view modals (hidden commerce UI, cf. shared log § Dropped compare tray), the promo rail's empty 6th slide (rendered by the block as a spacer), "Play/Pause Slides" hidden toggles, rail arrows.

## Images needing rehost (401/403 sources)
None — every authored source answers 200 (manifest + qa-gate 0 broken images in Chromium on the aem-up harness, 29/29 visible images loaded at 1440 and 360 after a scroll pass).

## Gates
| gate | result |
|---|---|
| `preflight-runtime.mjs --no-install` | exit 0 |
| `block-lint.mjs blocks/ --styles styles/styles.css` | exit 0 — 0 🔴 tree-wide; my blocks 0 🟡 (8 tree 🟡 = chrome, foundation-recorded) |
| `code-sync-verify.mjs --lint --files <8 block files>` | exit 0 — eslint 0 / stylelint 0 (tabs.css, cards.css carry a file-level `no-descending-specificity` disable with the reason: variant-grouped lifted cascade) |
| `davids-model-lint.mjs content/index.html --icons-dir icons --styles styles/styles.css` | exit 0 — PASS 0 🔴, 4 🟡: TEXT (25/40 alt="" — hero backgrounds + category tiles, decorative next to their labels), D4 SVG advisory (icons verified pure-vector), VARIANT-COLLIDE ×2 (`hero` / `content` tokens in styles.css on-dark button scoping — intended: `main .hero a.button.secondary` reaching `.carousel.hero` paints the same white outline `main .carousel …` already does) |
| `block-roundtrip` per block, served prototype `http://127.0.0.1:8814/index-proposed.html` (maps `carousel=#hero`, `tabs=.band--tabs`, `cards=.band:has(.rail), .band:has(.about__row)`, `columns=.band:has(.biz__row)`) | tabs exit 0 · columns exit 0 · carousel exit 2 (exactly 4 🔴 MISSING CTA = the 4 `aria-hidden` Splide clone duplicates of the 4 authored CTAs) · cards exit 2 (exactly 10 🔴 MISSING CTA, all inside the `display:none` quick-view modals: 4 "Shop Now" + 6 inline links) — EW 99/99 editable, 0 dead, 0 duplicated |
| `block-roundtrip` whole page `--strict`, served prototype | exit 2 — the same 14 🔴 (4 + 10), nothing else; EW editable 99/99 |
| `block-roundtrip` whole page `--strict`, drop-aligned reference `stardust/.work/deploy/_index-proto-ref.html` (schema-view minus exactly the generator's `--drop .splide__slide--clone, .quickView-modal` — nothing visible at rest removed; built by `_index-protoref.mjs`) | **exit 0** — 0 structural 🔴, EW editable 99/99, dead 0, duplicated 0, exempt 0; 🟡 IMG COUNT only (CSS backgrounds vs authored pictures) |
| `build-harness.mjs content/index.html stardust/.work/deploy/harness/index.html --root .` | exit 0 (marker `stardust-harness-60ba689b45c3`) |
| `aem up --port 3114` (port.mjs harness; started by this worker, stopped at hand-back) + `qa-gate.mjs <harness> --schema stardust/eds-schema/index.json` | **exit 0 — PASS 38 ok, 9 warn, 0 fail** (1 h1 in section #2, all blocks loaded + non-empty, 0 pageerrors, 0 broken images, unit counts 8/3/7/6/6/2/5 ✓, tabs 2–3 drive ✓; warns: chrome controls (not mine), active tab-1 inert by design, wide-1600 carousel 1600 / rails 1450 = the source's full-bleed hero + edge-running rails) |
| `ew-editability-probe.mjs --content content/index.html --simulate-editor --verbose` | exit 0 — authored 99 / editable 99 / dead 0 / dup 0; block Δh 0 everywhere; ONE reported edit-mode drift (`cards` "Learn More" 42 → 16 px) is a probe-mimic artefact: the probe's runtime mimic buttonizes ANY sole link in a `<p>` (probe line 154) while this project's `scripts.js` is formatted-only (runtime-contract) — on the aem-up harness the promo card's text link has no class and is 16 px in both states |
| `render-harness.mjs content/index.html stardust/.work/deploy/harness/index.png --fragments content/` | exit 0, block errors [] |
| `_index-harness-probe.mjs` (aem-up harness, 1440 + 360) | carousel list flex / bars grid(1440) flex(360), tab list + grids flex, 4 card lists flex, columns row flex (block at 360 = lifted); units 4 slides + 4 clones + 4 items, 3 tabs/panels, cards 7/6/6/5, 2 cols; 29/29 visible images loaded; 0 console/page errors; drives: progress item 2 → slide 2 (`translateX(-300%)`), tab 2 → aria-selected + hidden swap; hero 750 @1440 (progress 264 @ y583, header 97) / 552 @360 |
