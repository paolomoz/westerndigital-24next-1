# EDS conversion log — solutions worker rows (agent id `solutions`)

Merged into `stardust/eds-conversion-log.md` by the coordinator when the wave closes. Brief: `stardust/.work/deploy/briefs/solutions.md`. Progress: `stardust/.work/deploy/progress/solutions.log`. Helpers/probes: `stardust/.work/deploy/_solutions-*`. Inventory claim appended to the shared log § Block inventory (2026-09-21T12:1xZ).

## Inventory rows owned (claimed in the shared log § Block inventory)
| block | variants | tier | authored shape | status |
|---|---|---|---|---|
| `anchor-nav` | — | template-slotted | ONE row, ONE `<ul>` of in-page links (`#choose-your-solution`, `#explore-storage-solutions`, `#compare-and-learn`, `#faq` — hrefs verbatim); chevron glyph = CSS `::before` data URI | done |
| `tiles` | `usecase`, `explore`, `tile`, `buy` | reconstructive | one row per tile `[media \| text]` — usecase `[<p>:sol-…:</p> \| <p><strong>use case</strong></p> + <p><a>Solution</a></p>]` ×7; explore `[picture \| h3 + p + <p><a>Learn More</a></p>]` ×8; tile `[<p>:sol-…:</p> \| h3 + p + <p><a>Learn More</a></p>]` ×3 / ×6; buy (products) `[picture svg \| h3 + p (+ <p><a>…</a></p>)]` ×4. Section head (h2 [+ intro p], buy foot p) = default content styled in place | done |
| `table` | `compare` (+ `no-header` decode) | reconstructive | h2 default content; header row (4 `<p>`) + 5 rows × 4 cells (`<p><strong>` first column); compare = third column rendered as a pill | done |
| `accordion` | `faq` | reconstructive | h2 default content (REABSORBED into the panel head, EW8); one row per Q/A `[<p>question</p> \| answer <p>(s) with inline links]` ×10; row 1 open at rest | done |

Additive on a shared block (recorded): `blocks/columns/columns.css` — two appended `main .columns.cta …` rules (flex-wrap nowrap, label white-space normal): the source's two CTA buttons flex-SHRINK and their labels wrap (58 px, one row); the global `a.button { white-space: nowrap }` + the variant's `flex-wrap: wrap` stacked them into two rows (233 vs 187 px band — the page's only geometry delta before the fix). The cta variant's proof was delegated to this page by the index worker's log.

Additive on a locked input (FLAGGED for the coordinator): 16 pure-vector files `icons/sol-*.svg` (0.9–10 KB, viewBox 80/64, rendered 48 px; extracted from the prototype's inline SVGs by `_solutions-icons.mjs`). The per-tile icons are inline `<svg>` on the source with no URL, so `:sol-<slug>:` icon tokens are the only ingestible editorial form (schema note "icon (:name: or svg picture)"); the runtime fetches `/icons/<name>.svg` from the code origin. No existing file in `icons/` was touched.

## Blocks written (Step 8)
| block | files | decode / EW notes |
|---|---|---|
| `anchor-nav` | `blocks/anchor-nav/anchor-nav.{js,css}` | authored `<ul>` MOVED into `nav.chips` inside `.anchor-nav-wrap` (lifted `.contain`); items styled by descendant selectors; `main .section.anchor-nav-container { padding: 16px 0 }` = `.sol-nav` (breadcrumbs shares the section, its CSS carries no band padding). Runtime words: none (nav aria-label). |
| `tiles` | `blocks/tiles/tiles.{js,css}` | media cell = the cell whose only content is picture/img/`span.icon` (single-cell rows split the same way, #62/#104 `cellNodes`); text cell: heading → `.title`, whole-paragraph `<strong>` → `.title` (usecase), sole-link `<p>` → `.actions` (moved, EW3; text links, runtime buttonisation is formatted-only), rest → `.copy`. Section `data-id` (section-metadata `id` row) copied onto the section id → anchor chips land. Band + head in place (`main .section.tiles-container`, `.default-content-wrapper`), buy variant on `:has(.tiles.buy)` with the products stepped container. Icons authored as `:sol-*:` tokens. |
| `table` | `blocks/table/table.{js,css}` | rows → `<table>`: row 1 → `thead th[scope=col]` (unless `no-header`), others → `td`; cell nodes MOVED (harness-only bare-text fallback). compare: `td[2]` content wrapped in `div.tag` (inline pill; `.tag :is(div, p) { display: inline }` keeps the editor's wrapper divs inline — edit-mode Δh was 210 px before). Striped rows by `tr:nth-child` on generated rows. |
| `accordion` | `blocks/accordion/accordion.{js,css}` | EW7: question `<p>` moves into `div.q-title`; `button.toggle` (aria-expanded / aria-controls / aria-label, two inline 16×16 svgs plus/minus) is glyph-only; the whole `.q-row` toggles; row 1 open at rest (recorded live state). Head reabsorbed from the section's default-content wrapper (children MOVED, wrapper removed — the title sits inside the grey panel on the source). `.a.collapsed` = source `.hidden` (max-height/opacity .4s, display block kept so the collapse animates; `prefers-reduced-motion` → no transition). `.list` is a column flex container (QA-gate unit proxy; 0 px change). Runtime words: aria-labels only. |

Shared helpers copied per block (no shared import): `el`, `cellNodes`, `wrapNode`. Nothing measured in `decorate()`; no words added to the DOM.

## Content page `content/solutions.html` (Step 9)
- Generated by `prototype-to-content.mjs` from `stardust/.work/deploy/schema-view/solutions.html` (the only document carrying `<main>` + `data-section`; the served prototype has neither) with `--map` for all 10 sections (`hero=block:hero page-nav=block:breadcrumbs choose|explore|industry|compare-learn=block:tiles compare-table=block:table faq=block:accordion innovation=block:hero cta=block:columns`), `--drop '#contact-form' --drop '#ProductCompareTray' --drop '.css-container'` (decided-out modal / compare tray / empty author-mode shells), `--slug solutions`; ledger `stardust/.work/deploy/transcribe.json`.
- **Patches** `stardust/patches/solutions.json` (10 `replace` ops, one per section, computed by `stardust/.work/deploy/_solutions-patches.mjs` from the schema-view DOM + `_media-manifest.json` + captured `<title>`/meta description; applied LAST by the generator). Why: the generator's repeat-unit emitter cannot express the locked shapes (icon tokens as a media cell, FAQ questions inside non-authorable `<button>`s, two blocks in the page-nav section, one-row hero/band/cta shapes, variant classes, CSS-background photos as editorial pictures, the captured title). Section selectors are content-unique against the generator's pre-patch output (`:nth-child` is not supported by the patch engine): `main > div:has(h1)`, `:has(div.breadcrumbs)`, `:has(div.tiles h2)`, `:has(div.tiles img)`, `:has(a[href^="/explore/"])`, `:has(a[href="/solutions/hdd-vs-ssd"])`, `:has(div.table)`, `:has(div.accordion)`, `main > div:has(h1) ~ div:has(div.hero)`, `:has(div.columns)`. Re-run: `node stardust/.work/deploy/_solutions-patches.mjs && node …/prototype-to-content.mjs <same args>` (exact command in `progress/solutions.log`).
- 10 sections: [metadata + hero.photo] · [breadcrumbs + anchor-nav] · [h2 + p + tiles.usecase + id] · [h2 + tiles.explore + id] · [h2 + tiles.tile] · [h2 + tiles.tile + id] · [h2 + table.compare] · [h2 + accordion.faq + id] · [hero.band] · [columns.cta]. Metadata: Title = captured `<title>` "Discover How Hard Drives Can Transform Your Data Storage | WD", Description = captured meta description. One `<h1>`.
- In-page anchors: section-metadata `id` rows (`choose-your-solution`, `explore-storage-solutions`, `compare-and-learn`, `faq`) — the runtime maps them to `data-id`; the owning blocks copy `data-id` → `id` (the pipeline may set `id` directly; either way one id). Verified: all 4 ids present, chip `#faq` scrolls to the FAQ band.
- Images: 10 authored `<img>` — 8 explore cards (captured 1280 webp renditions, source alt kept), hero + innovation backgrounds (captured 3000 renditions, alt="" — behind text). Every src = the CAPTURED absolute URL: 9 from `_media-manifest.json` (status 200); the NAS-enclosure card image has no manifest row (the crawl missed it) — its URL is the one recorded in `stardust/replica/capture/lift/solutions-1440.json` / `migrated/solutions/_meta.json`, verified 200 image/webp with a browser UA (10.6 KB vs the 10.9 KB local copy — CDN re-encode of the same rendition).
- Links: `/` (breadcrumb Home, source `/en-us`); 4 in-page anchors verbatim; every other href absolute on `https://www.westerndigital.com` (solution / product / explore pages — links decision row). "Contact a Sales Representatives" (`#contact-form` modal trigger on the source) → `https://www.westerndigital.com/company/contact-us` (conversion log § Dropped: decided-out form backend).
- Not authored (decisions): `#contact-form` modal copy, compare tray, author-mode shells; the FAQ toggle glyphs and chip chevron are UI glyphs (block-owned SVG / CSS).

## Images needing rehost (401/403)
None found: 9/10 manifest 200 (in-page); the 10th (NAS enclosure) 200 with a browser UA. Capture technique `headless`; the definitive pre-flight is the coordinator's `media-reconcile.mjs` / `rehost-media.mjs` run. Advisory: none (no authored SVG media — icons are `:sol-*:` tokens under `icons/`).

## Gates
| gate | result |
|---|---|
| `preflight-runtime.mjs --no-install` | exit 0 |
| `block-lint.mjs blocks/ --styles styles/styles.css` | exit 0 — 0 🔴 tree-wide; my blocks 0 🟡 (8 tree 🟡 = chrome, foundation-recorded) |
| `code-sync-verify.mjs --lint --files <9 files>` (4 blocks × js+css + columns.css) | exit 0 — eslint 0 / stylelint 0 |
| `davids-model-lint.mjs content/solutions.html --icons-dir icons --styles styles/styles.css` | exit 0 — PASS 0 🔴, 2 🟡: D1 BREADCRUMB (accepted, shared log § Step 2); D1 "anchor-nav single-row prose block" — justified: a bespoke chip widget (in-page nav, one `<ul>`), not prose |
| `block-roundtrip` `--blocks anchor-nav,tiles,table,accordion` vs the drop-aligned reference `stardust/.work/deploy/_solutions-proto-ref.html` (schema view minus the 3 `--drop`s; maps `tiles=[data-section=choose], [data-section=explore], [data-section=industry], [data-section=compare-learn]`, `table=[data-section=compare-table]`, `accordion=[data-section=faq]`, `anchor-nav=.sol-chips`, `breadcrumbs=.sol-crumbs`, `hero=[data-section=hero], [data-section=innovation]`, `columns=[data-section=cta]`) | **exit 0** — 0 structural 🔴; EW editable 117/117 (anchor-nav 1, tiles 16/25/10/19, table 25, accordion 21), dead 0, duplicated 0, exempt 0 |
| whole-page `block-roundtrip --strict` (same reference + maps) | **exit 0** — 0 🔴; EW 126/126; 🟡 IMG COUNT ×2 on hero.photo / hero.band only (CSS background on the prototype vs editorial picture — same as the products worker recorded) |
| regression: whole-page `block-roundtrip --strict` on `content/index.html` vs `_index-proto-ref.html` (index worker's maps) after the columns.css append | exit 0 — 0 🔴, EW 99/99 |
| `block-roundtrip … --blocks tiles` on `content/products.html` vs `_products-view.html` (`tiles=[data-section=buy-direct]`) — the `buy` variant on the products worker's rows | exit 0 — 0 🔴, EW 13/13 |
| `ew-editability-probe.mjs --content content/solutions.html --simulate-editor --verbose` | exit 0 — authored 126 / editable 126 / dead 0 / dup 0; drift 0, editDrift 0 on every block; blockΔh 0 on every block (`table` was 210 before the `.tag :is(div, p) { display: inline }` rule; re-probe 0) |
| `ew-editability-probe.mjs --content content/products.html --simulate-editor` | exit 0 — 93/93; `tiles` 11/11, drift 0, Δh 0 (the products worker's pre-implementation drift note is closed) |
| `render-harness.mjs content/solutions.html … --fragments content/` | exit 0 — block errors [] → `stardust/.work/deploy/harness/solutions.png` |
| `build-harness.mjs` + `aem up --port 3114` (port.mjs harness; STARTED by this worker — port.mjs reported a reusable sibling server but no listener existed — and stopped at hand-back) + `qa-gate.mjs … --schema stardust/eds-schema/solutions.json` | **exit 1 — 52 ok, 5 warn, 5 fail**; every fail is `units:` on a WRONG block: the gate pairs schema sections with page BLOCK INSTANCES by order (`instances[pos]`), and the page-nav section holds two blocks (`breadcrumbs` + `anchor-nav`, the locked shape), so every check after it is shifted by one (`"choose" → block anchor-nav`, `"faq" → block table`, …). Correctly paired counts are proven by `_solutions-harness-probe.mjs` (below). Everything else green: 1 h1, 0 pageerrors, 0 broken images, all 13 blocks loaded + non-empty, accordion drive 10/10 `aria-expanded changed`, h1Section ok. Warns: 4 dead header controls (foundation), wide-1600 hero full-bleed (correct: bg full-bleed, content re-contained). **Coordinator note (gate gap):** a section with `block: [a, b]` in the schema should consume two instances; a `table` block's `<tr>` units are not a tag proxy either. |
| `_solutions-harness-probe.mjs` (aem-up harness, 1440 + 360) | 0 console/page errors; chips flex, 4 tile grids flex 7/8/3/6, usecase inner flex, faq row flex (block at 360 = lifted), 10 items, 5 table rows, 5 pills inline; 26/26 (1440) and 25/26 visible (360, one below the fold at scroll end) images `clientWidth > 0`, 0 `naturalWidth 0`; 16/16 icons 48×48 with loaded img; 4 anchor ids; FAQ drive: item 2 opens (0 → 144/264 px), item 1 independent; **every band y/h == the served prototype at both widths** (hero 600/495, page-nav 169/296, choose 856/1596, explore 2102/5041, industry 581/1189, compare-learn 905/2032, table 547/554, faq 1179/1369, band 528/366, cta 187/187 after the columns fix); `main` 7653 / 13127 == prototype |

## Decisions / residuals (D8 ledger)
- Contact-form modal not converted (shared log § Dropped); the CTA bounces to the source contact page.
- `breadcrumbs` uses the products stepped container (576/768/992/1140) while the solutions prototype uses the fixed 1140 `.contain` — identical at ≤767 and ≥1140, ≤16 px per side between (read-only block, noted).
- Industry tile "schools and research<br> environments": the source's editorial `<br>` in body copy is kept.
- The generator's ledger `blocks[]` lists hero/breadcrumbs/tiles/table/accordion/columns — `anchor-nav` is missing because a section maps to ONE block (coordinator gap already noted by the products worker: multi-block sections).
- qa-gate `units` pairing (above) — gate gap, not a page defect.
- `stardust/.work/harness.pid` was rewritten by this worker's launcher (it had recorded its own short-lived npm pid) and repaired to the live listener pid before `port.mjs stop harness`.
