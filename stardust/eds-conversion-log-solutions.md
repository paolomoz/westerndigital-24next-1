# EDS conversion log — solutions worker rows (agent id `solutions`)

Merged into `stardust/eds-conversion-log.md` by the coordinator when the wave closes. Brief: `stardust/.work/deploy/briefs/solutions.md`. Progress: `stardust/.work/deploy/progress/solutions.log`. Helpers/probes: `stardust/.work/deploy/_solutions-*`.

## Inventory rows owned (claimed in the shared log § Block inventory)
| block | variants | tier | authored shape | status |
|---|---|---|---|---|
| `anchor-nav` | — | template-slotted | one row, one `<ul>` of in-page links (chips) | pending |
| `tiles` | `usecase`, `explore`, `tile`, `buy` | reconstructive | one row per tile [media \| text] — per variant, see § Blocks | pending |
| `table` | `compare` | reconstructive | header row + 5 data rows | pending |
| `accordion` | `faq` | reconstructive | one row per Q/A [question \| answer] | pending |

## Blocks written (Step 8)
(pending)

## Content page `content/solutions.html` (Step 9)
(pending)

## Images needing rehost (401/403)
(pending)

## Gates
| gate | result |
|---|---|
| `preflight-runtime.mjs --no-install` | exit 0 |

## Decisions / residuals (D8 ledger)
(pending)
