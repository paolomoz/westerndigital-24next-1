# EDS conversion log — company worker rows (agent id `company`)

Merged into `stardust/eds-conversion-log.md` by the coordinator when the wave closes. Brief: `stardust/.work/deploy/briefs/company.md`. Progress: `stardust/.work/deploy/progress/company.log`. Inventory claim appended to the shared log § Block inventory (2026-09-21T12:06Z). Helpers/probes: `stardust/.work/deploy/_company-*`.

## Inventory rows owned
| block | variants | tier | authored shape | status |
|---|---|---|---|---|
| `company-cards` | `values`, `overview` | reconstructive | values: h2 default content before the block (reabsorbed, EW8 — the head sits on the photo ground); row 1 [picture] = band background (editorial); one row per value [picture icon \| h3 + p] — 1 + 5 rows. overview: one row per card [picture \| h3 + p + `<p><em><a>`] — 3 + 3 rows | done |

Shared blocks used as authored content only (no variant added, nothing edited): `hero` · `split` (products worker), `columns` · `split` (index worker).

## Block notes (decode, EW, deviations recorded)
- **company-cards** — one decode, two skins. Rows classified by content (#42/#48): values — a picture-only row before any unit = background layer `.bg`; a leading text-only row with an h2 = in-table head (defensive fallback; the locked shape authors the head as default content and `decorate()` MOVES the `.default-content-wrapper` children into `.head`, removes the wrapper — EW8, verified: 0 wrappers left in the section, h2 editable); every other row = one unit; a single-row block with ≥2 unit headings is segmented on the most frequent heading tag with a text-less picture travelling to the unit its heading opens (#52/#73). Unit: picture → `.media`, heading → `.title`, link-only `<p>` → `.actions` (moved, EW3), rest → `.body`; `.content` = source `.co-card__body` (padding 24). No words added (#100), nothing measured in `decorate()`, no `@ew-exempt`.
- **values CSS** — lifted verbatim from company.css `.co-values*`/`.co-value*`/`.co-h2--bold`/`.co-h5`/`.co-sub`: band padding 48/64, white text, `.list` flex/space-around/-16 margins, `.item` 50 % → 33.33 % (768–991) → 16.67 % (≥992), icon `max-height 100 / padding 12 0 / margin auto`, the source's inline `.divider-5-3-2 > div::before` bars (4 × 125 white) reproduced per breakpoint on the GENERATED `.item` (`:nth-child` on wrappers only, EW2). Background = the authored picture as an absolute cover layer; `background-color: var(--c-navy)` as the no-image fallback (white text stays readable — deviation only when the author removes the bg row).
- **overview CSS** — lifted from `.co-cards__row`/`.co-card-col`/`.co-card`/`.co-card__*`/`.co-h4`/`.co-btn`: grey `--c-grey-1` rounded 16 cards, 24 px body, 3-up ≥992 (stacked, 16 px gaps below), h3 24/26/28 · 500 · mb 8, outline button 8 16 → 12 40, mt 16 → 24, mb 36 → 0, mr 24 ≥768 painted on `.actions a:any-link` (variant-owned, #25).
- **Band rhythm the source carried on its ONE `.co-lead` section** (three content-anchored rules at `.section` specificity in company-cards.css — spacer ladder step 2, D8): the section after the values band gets `.co-lead` padding-top 48/64; an overview grid followed by another section keeps `.co-cards__row--padded` pb 48/64 (its −16 margin collapses to 0 against `.co-cards--first` mb 16); the page's last section carries `.co-lead` pb 48/64 + its 32 px margin-bottom (80/96). The index worker's columns block deliberately left this to the page owner.
- **CTA marks**: columns split = `<p><strong><a>` (locked triage row; the columns block paints the outline whatever the mark); company-cards = `<p><em><a>` — the source `.co-btn` is an outline button = the foundation's `secondary` (D6), so the edit-mode repaint matches the block paint (probe drift 0). Coordinator note: the same visual button carries two marks on one page (`strong` in columns per the lock, `em` here) — harmonising to `em` in the columns lock would be the consistent authoring story.
- **hero split** (shared, authored only): picture 1 = the 3000 cascade bg; NO picture 2 — the <768 mobile rendition (`wd-abstract-cascading-glass-plates-dark-blue…840`) is a live 404 (brief), so `.hero-media-inner` stays empty and the black band shows at 360 (probe: `hero-bg display none`, bg #000, 456 px = proto). The h1's layout `<br>` ("Powering Data,<br>Driving Potential") dropped (HBR).

## Content page `content/company.html`
- 7 sections: [metadata + hero split] · [vision default content + section-metadata `intro`] · [h2 + company-cards values] · [columns split: text | picture] · [company-cards overview ×3] · [columns split: picture | text] · [company-cards overview ×3]. One `<h1>`. Title / Description = the CAPTURED `<title>` / meta description (`stardust/current/pages/company.json`).
- **Generator recipe (reproducible):** `node stardust/.work/deploy/_company-patches.mjs` → `stardust/patches/company.json` (7 `replace` ops, one per section, transcribed from the schema-view DOM + `_media-manifest.json` + captured title); then `node $PLUGIN/skills/deploy/scripts/prototype-to-content.mjs stardust/.work/deploy/schema-view/company.html --schema stardust/eds-schema/company.json --out content/company.html --slug company --map hero=block:hero --map vision=default --map values=block:company-cards --map leadership=block:columns --map overview-cards=block:company-cards --map responsibility=block:columns --map more-cards=block:company-cards --drop '#ProductCompareTray'` (`--force` was needed once, over the skeleton shell). Input = the schema view (the served prototype carries no `<main>`/`data-section`); the served prototype `http://127.0.0.1:8814/company-proposed.html` is the round-trip baseline (the hidden compare tray sits outside every mapped selector, so no drop-aligned reference was needed).
  - Why patches: the emitter leaves relative `assets/media` srcs, nested `<p><p>` CTAs, trailing/heading `<br>`s, no variant classes, no bg/media picture cells for CSS-background photos, no section-metadata, the `more-cards` prose duplicated before its block, and a Title derived from the h1 (recorded already by the index/products workers as generator gaps: variant token on `--map`, captured-title metadata, section-metadata emission).
- Images: 15 authored `<img>` (hero bg 3000, values bg 3000, 5 icons 1280, leadership 3000, 6 card images 1280, corporate-responsibility 3000) — every src = the CAPTURED absolute URL (manifest status 200 for all 15). The prototype's 500-px icon / 840-px mobile renditions are not in the manifest → not authored (never a guessed URL, #118); the pipeline generates responsive variants. Alt: icons/cards take the source `title` text ("icon handshake", "innovation", …) where the source alt was empty (D13); background photos alt="".
- Links: `/company/*`, `/careers` → absolute on `https://www.westerndigital.com` (unmigrated targets, links decision row); `https://investor.wdc.com/` kept; no link to the four migrated pages occurs in the page body. `&nbsp;` in "At WD our vision" kept byte-for-byte (`sanitise.js`).
- Not authored (decisions): `#ProductCompareTray` (shared log § Dropped), the hero mobile rendition (404), the source `aria-label`s on the buttons ("Learn More about …" — the visible label is authored; a11y label enrichment is a follow-up, not content).

## Images needing rehost (401/403)
- None: 15/15 manifest status 200 (in-page capture), qa-gate 0 broken images, harness probe 15/15 loaded at 1440 (14 visible at 360 — the hero bg is `display:none` there by design). Advisories for `media-reconcile` / `rehost-media`: `company-overview-corporate-reponsibility.png.wdthumb.3000.3000.webp` was captured as a **2.9 MB PNG** (local `…-2ca83692.png`) under a `.webp` URL → `oversize` (> 1 MB) → CDN pre-shrink / `--resize`; `company-overview-innovation.png.wdthumb.1280.1280.webp` captured as a 436 KB PNG under a `.webp` name (mime/extension mismatch to verify, #118).

## Gates
| gate | result |
|---|---|
| `preflight-runtime.mjs --no-install` | exit 0 |
| `block-lint.mjs blocks/ --styles styles/styles.css` | exit 0 — 0 🔴 tree-wide; company-cards 0 🟡 (the 8 tree 🟡 are the chrome blocks, foundation-recorded) |
| `block-roundtrip.mjs` served prototype, per block — `company-cards` (`--map "company-cards=.co-values, .co-cards--first, .co-cards__row:not(.co-cards__row--padded)"`) · `columns` (`.co-lead__row, .co-cr__row`) · `hero` (`.co-hero`) | exit 0 / 0 / 0 — 0 structural 🔴; EW 29/29 · 6/6 · 2/2, dead 0, dup 0, exempt 0; 🟡 IMG COUNT only (CSS backgrounds vs authored pictures) |
| whole-page `block-roundtrip` `--strict` (`--ew` on), served prototype | **exit 0** — 0 🔴, EW editable 37/37, dead 0, duplicated 0, exempt 0 |
| `davids-model-lint.mjs content/company.html --icons-dir icons --styles styles/styles.css` | exit 0 — PASS 0 🔴, 2 🟡: D1 hero "prose-only 2-row block" (the locked shared `hero` block, bg picture + h1/p — accepted), D3 company-cards rows with 1 vs 2 cells (the values background row — locked shape, accepted) |
| `build-harness.mjs content/company.html stardust/.work/deploy/harness/company.html --root .` | exit 0 (marker `stardust-harness-60ba689b45c3`, file `stardust/.work/deploy/harness/marker.txt`) |
| `aem up` port 3114 (port.mjs slot; started by this worker, later stopped by a sibling's `port.mjs stop harness` and restarted by them — reused, NOT stopped by me) + `qa-gate.mjs … --schema stardust/eds-schema/company.json --marker stardust-harness-60ba689b45c3` | **exit 1 — 28 ok, 5 warn, 2 fail**. Identity asserted; 1 h1, 0 pageerrors, 0 broken images, every block loaded + non-empty, h1Section ok, 6 controls driven (4 dead = header, foundation). The 2 fails are a qa-gate PAIRING gap, not a render defect: it pairs schema section index i with block instance i "by ORDER over all schema sections", and `vision` (index 1) is DEFAULT CONTENT with no block, so `values`(2) is compared with block #2 = `columns` (2 cols ≥ 5 ✗) and `overview-cards`(4) with block #4 = `columns` (✗); `more-cards`(6) falls off the end (warn). Real counts (round-trip + probe): values 5, overview 3, more 3. Fix belongs in the tool (skip `defaultContent: true` sections when building the position map, or pair by `schema.block`) — request to the coordinator. |
| `ew-editability-probe.mjs --content content/company.html --simulate-editor --verbose` | exit 0 — authored 40 / editable 40 / dead 0 / dup 0; drift 0, editDrift 0, blockΔh 0 for hero, default, company-cards (28), columns |
| `render-harness.mjs content/company.html stardust/.work/deploy/harness/company.png --fragments content/` | exit 0 — block errors [] |
| `_company-harness-probe.mjs` (aem-up harness vs served prototype, 1440 + 360, fonts settled, scrolled) | 0 console/page errors; hero row / values list / overview lists / columns rows all `flex`; units 5 / 3 / 3, 2 cols × 2, values head reabsorbed (0 default-content wrappers left), 15/15 visible images loaded, 0 zero-width. **Block heights = prototype at both widths**: hero 500/456, values 436/809, leadership 464/621, cards 501/1419, responsibility 464/564, last cards 437/1323; icon boxes identical (76×100 … 98×100). Page: main 3372 vs 3344 @1440 (5986 vs 5958 @360) = +32 (the source `.co-lead` margin-bottom, which sits OUTSIDE its `main` height and is carried here as last-section padding — footer lands at the same y) −4 (foundation `intro` band 410 vs 414 @1440, 665 vs 669 @360 — foundation-owned, noted for the coordinator). |
| `code-sync-verify.mjs --lint --files <6 files>` | exit 0 (after max-len / comment-spacing wraps; eslint 0, stylelint 0) |
| regression: whole-page `block-roundtrip --strict` on `content/index.html` vs `_index-proto-ref.html` (index worker's maps) | exit 0 — 0 🔴, EW 99/99 (unchanged) |

## Decisions / residuals (D8 ledger)
- qa-gate `units` ×2 FAIL = tool pairing gap with a block-less default-content section (above) — the only non-green gate; counts proven elsewhere.
- Foundation `intro` band −4 px vs the prototype (h2/p rhythm) — read-only, reported.
- `.co-lead` 32 px margin-bottom carried as the last section's padding-bottom (80/96) — same footer position.
- Corporate-responsibility 3000 rendition is a 2.9 MB PNG → coordinator's `rehost-media` oversize path.
- Hero mobile rendition not authored (live 404) → solid #000 below 768 (brief).
- Card `aria-label`s of the source buttons not carried (visible labels authored).
